name: Android CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build APK Samostalno
      run: |
        mkdir -p bin classes
        export ANDROID_SDK_ROOT=/usr/local/lib/android/sdk
        
        # Pronalaženje točne lokacije manifesta i automatska sterilizacija skrivenih znakova
        MANIFEST_PATH=$(find . -name "AndroidManifest.xml" | head -n 1)
        RES_PATH=$(find . -name "res" -type d | head -n 1)
        
        # Linux naredbe koje prisilno čiste sve skrivene razmake i popravljaju navodnike
        sed -i 's/\xe2\x80\x9c/"/g' $MANIFEST_PATH
        sed -i 's/\xe2\x80\x9d/"/g' $MANIFEST_PATH
        sed -i 's/\xc2\xa0/ /g' $MANIFEST_PATH
        
        # Ponovno generiranje čistog izvora bez skrivenih grešaka pri kopiranju
        find . -name "*.java" > sources.txt
        javac --release 8 -d classes -classpath $ANDROID_SDK_ROOT/platforms/android-34/android.jar @sources.txt
        
        $ANDROID_SDK_ROOT/build-tools/34.0.0/d8 --release --output bin classes/com/helper/eightball/*.class
        
        # Pakiranje resursa pomoću očišćenog manifesta
        $ANDROID_SDK_ROOT/build-tools/34.0.0/aapt package -f -M $MANIFEST_PATH -S $RES_PATH -I $ANDROID_SDK_ROOT/platforms/android-34/android.jar -F bin/8ballHelper-unsigned.apk bin/
        
        cp bin/8ballHelper-unsigned.apk bin/8ballHelper.apk

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: 8ballHelper-APK
        path: bin/8ballHelper.apk
